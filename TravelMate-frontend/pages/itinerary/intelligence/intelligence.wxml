<view class="container">
  <image class="bg-image" src="/images/background.png" mode="aspectFill"></image>
  <view class="content">
    <view wx:if="{{loading}}" class="loading">加载中...</view>
    <rich-text wx:else nodes="{{nodes}}" />
  </view>
</view>